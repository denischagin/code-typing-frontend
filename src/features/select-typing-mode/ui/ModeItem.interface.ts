import {TileProps} from "@shared/ui/tile";

export interface ModeItemProps extends TileProps{
    isActive?: boolean;
}

export interface ModeSmallItemProps extends TileProps{
    isActive?: boolean;
}