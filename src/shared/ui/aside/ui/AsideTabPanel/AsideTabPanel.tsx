import {Box} from "@chakra-ui/react";

import {AsideTabPanelProps} from "@shared/ui/aside";

export const AsideTabPanel = (props: AsideTabPanelProps) => {
    return (
        <Box {...props} />
    )
}